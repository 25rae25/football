"use client";

import styled from "@emotion/styled";

const MainWrapper = styled.div`
  max-width: 1024px;
  margin: 0 auto;
`;

export default function MainContainer() {
  return (
    <MainWrapper>
      <ul>
        <li></li>
      </ul>
    </MainWrapper>
  );
}
